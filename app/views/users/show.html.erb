<% provide(:title, @user.name) %>
<% if current_user.isTeacher? && current_user.name == @user.name%>
  <%= render 'person' %>
<% end %>
<% if !current_user.isTeacher? && current_user.name == @user.name%>
  <%= render 'student' %>
<% end %>